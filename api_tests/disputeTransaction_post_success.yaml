# Test generated by RoostGPT for test ArtilleryTest1 using AI Type Open AI and AI Model gpt-4


# Test generated for /disputeTransaction_post for http method type POST in artillery framework

# RoostTestHash=a16706fdab


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "disputeTransaction_post_success.csv"
    fields: ["customerId", "emailId", "transactionId", "reasonCode"]
    skipHeader: true
  headers:
    Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
    koreUserId: "{{$processEnvironment.koreUserId}}"
    botId: "{{$processEnvironment.botId}}"
    accountId: "{{$processEnvironment.accountId}}"

scenarios:
  - name: "Dispute a transaction"
    flow:
      - post:
          url: "/disputeTransaction"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            koreUserId: "{{$processEnvironment.koreUserId}}"
            botId: "{{$processEnvironment.botId}}"
            accountId: "{{$processEnvironment.accountId}}"
          json:
            customerId: "{{ customerId }}"
            emailId: "{{ emailId }}"
            transactionsData:
              - transactionId: "{{ transactionId }}"
                reasonCode: "{{ reasonCode }}"
          expect:
            - statusCode: 200
            - hasProperty: "description"
            - equals: ["description", "Dispute Transactions reported successfully"]
      - think: 1

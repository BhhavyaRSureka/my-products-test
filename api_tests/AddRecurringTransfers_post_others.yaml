# Test generated by RoostGPT for test ArtilleryTest1 using AI Type Open AI and AI Model gpt-4


# Test generated for /AddRecurringTransfers_post for http method type POST in artillery framework

# RoostTestHash=b9ae237532


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "AddRecurringTransfers_post_others.csv"
    fields:
      - "customerId"
      - "amount"
      - "sourceAccountId"
      - "targetAccountId"
      - "currency"
      - "status"
      - "scheduledDate"
      - "amountType"
      - "iterate"
      - "numberOfTransfers"
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      headers:
        koreUserId: "7890"
        botId: "1234"
        userCode: "5678"
        accountId: "9012"
        Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Test scenario for non-success API responses"
    flow:
      - post:
          url: "/AddRecurringTransfers"
          headers:
            koreUserId: "7890"
            botId: "1234"
            userCode: "5678"
            accountId: "9012"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            currency: "{{ currency }}"
            status: "{{ status }}"
            scheduledDate: "{{ scheduledDate }}"
            recurringOptions:
              amountType: "{{ amountType }}"
              frequency:
                iterate: "{{ iterate }}"
                duration:
                  numberOfTransfers: "{{ numberOfTransfers }}"
          expect:
            - statusCode: 422
            - hasProperty: "errors"
            - hasProperty: "message"
          log: "Testing with invalid payload"
      - think: 1
      - post:
          url: "/InvalidURL"
          headers:
            koreUserId: "7890"
            botId: "1234"
            userCode: "5678"
            accountId: "9012"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            currency: "{{ currency }}"
            status: "{{ status }}"
            scheduledDate: "{{ scheduledDate }}"
            recurringOptions:
              amountType: "{{ amountType }}"
              frequency:
                iterate: "{{ iterate }}"
                duration:
                  numberOfTransfers: "{{ numberOfTransfers }}"
          expect:
            - statusCode: 404
          log: "Testing with invalid URL"
      - think: 1
      - post:
          url: "/AddRecurringTransfers"
          headers:
            koreUserId: "7890"
            botId: "1234"
            userCode: "5678"
            accountId: "9012"
            Authorization: "InvalidToken"
          json:
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            currency: "{{ currency }}"
            status: "{{ status }}"
            scheduledDate: "{{ scheduledDate }}"
            recurringOptions:
              amountType: "{{ amountType }}"
              frequency:
                iterate: "{{ iterate }}"
                duration:
                  numberOfTransfers: "{{ numberOfTransfers }}"
          expect:
            - statusCode: 401
            - hasProperty: "errors"
            - hasProperty: "message"
          log: "Testing with invalid Authorization token"

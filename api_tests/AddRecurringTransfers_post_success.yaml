# Test generated by RoostGPT for test ArtilleryTest1 using AI Type Open AI and AI Model gpt-4


# Test generated for /AddRecurringTransfers_post for http method type POST in artillery framework

# RoostTestHash=b9ae237532


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "AddRecurringTransfers_post_success.csv"
    fields:
      - "customerId"
      - "amount"
      - "sourceAccountId"
      - "targetAccountId"
      - "currency"
      - "status"
      - "scheduledDate"
      - "recurringOptions"
    skipHeader: true
  headers:
    Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
    koreUserId: "{{$processEnvironment.KORE_USER_ID}}"
    botId: "{{$processEnvironment.BOT_ID}}"
    userCode: "{{$processEnvironment.USER_CODE}}"
    accountId: "{{$processEnvironment.ACCOUNT_ID}}"

scenarios:
  - name: "Add Recurring Transfers"
    flow:
      - post:
          url: "/AddRecurringTransfers"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            koreUserId: "{{$processEnvironment.KORE_USER_ID}}"
            botId: "{{$processEnvironment.BOT_ID}}"
            userCode: "{{$processEnvironment.USER_CODE}}"
            accountId: "{{$processEnvironment.ACCOUNT_ID}}"
          json:
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            currency: "{{ currency }}"
            status: "{{ status }}"
            scheduledDate: "{{ scheduledDate }}"
            recurringOptions: "{{ recurringOptions }}"
          expect:
            - statusCode: 200
            - hasProperty: "customerId"
            - hasProperty: "amount"
            - hasProperty: "sourceAccountId"
            - hasProperty: "targetAccountId"
            - hasProperty: "currency"
            - hasProperty: "status"
            - hasProperty: "scheduledDate"
            - hasProperty: "recurringOptions"
      - think: 1

# Test generated by RoostGPT for test ArtilleryTest1 using AI Type Open AI and AI Model gpt-4


# Test generated for /UpdateCustomerInfo_put for http method type PUT in artillery framework

# RoostTestHash=89ddb90785


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "UpdateCustomerInfo_put_success.csv"
    fields: ["customerId", "email", "alternatePhoneNumber", "isAlternateMobileNumber", "phone", "isPrimaryMobileNumber", "location"]
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Update customer information"
    flow:
      - log: "Updating customer information"
      - put:
          url: "/UpdateCustomerInfo"
          headers:
            koreUserId: "{{$processEnvironment.AUTH_TOKEN}}"
            botId: "{{$processEnvironment.AUTH_TOKEN}}"
            accountId: "{{$processEnvironment.AUTH_TOKEN}}"
            Authorization: "{{$processEnvironment.AUTH_TOKEN}}"
            "X-Auth-token": "{{$processEnvironment.AUTH_TOKEN}}"
            "X-CORRELATION-ID": "{{$randomNumber(1000000000, 9999999999)}}"
          json:
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            alternatePhoneNumber: "{{ alternatePhoneNumber }}"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
          expect:
            - statusCode: 200
            - hasProperty: "customerId"
            - hasProperty: "email"
            - hasProperty: "alternatePhoneNumber"
            - hasProperty: "isAlternateMobileNumber"
            - hasProperty: "phone"
            - hasProperty: "isPrimaryMobileNumber"
            - hasProperty: "location"
      - think: 1

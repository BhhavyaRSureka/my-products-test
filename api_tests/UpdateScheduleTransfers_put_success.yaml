# Test generated by RoostGPT for test ArtilleryTest1 using AI Type Open AI and AI Model gpt-4


# Test generated for /UpdateScheduleTransfers_put for http method type PUT in artillery framework

# RoostTestHash=c0505709a3


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "UpdateScheduleTransfers_put_success.csv"
    fields:
      - "paymentId"
      - "customerId"
      - "amount"
      - "scheduledDate"
      - "sourceAccountId"
      - "targetAccountId"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Update Schedule Transfers Scenario"
    flow:
      - log: "Starting Update Schedule Transfers Scenario"
      - put:
          url: "/UpdateScheduleTransfers"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            paymentId: "{{ paymentId }}"
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            scheduledDate: "{{ scheduledDate }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
          expect:
            - statusCode: 200
            - hasProperty: "paymentId"
            - hasProperty: "customerId"
            - hasProperty: "amount"
            - hasProperty: "scheduledDate"
            - hasProperty: "sourceAccountId"
            - hasProperty: "targetAccountId"
      - think: 1

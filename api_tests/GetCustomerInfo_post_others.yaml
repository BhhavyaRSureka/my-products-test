# Test generated by RoostGPT for test ArtilleryTest1 using AI Type Open AI and AI Model gpt-4


# Test generated for /GetCustomerInfo_post for http method type POST in artillery framework

# RoostTestHash=88810bf424


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "GetCustomerInfo_post_others.csv"
    fields:
      - "customerId"
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
      auth_token: "{{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: "Test non-success API responses"
    flow:
      - post:
          url: "/GetCustomerInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $environment.auth_token }}"
            X-Auth-token: "{{ X-Auth-token }}"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json:
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - equals: ["error.message", "Invalid customerId"]
          # Comment: Expect a 400 status code and an error message due to an invalid customerId
          think: 1
      - post:
          url: "/InvalidURL"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $environment.auth_token }}"
            X-Auth-token: "{{ X-Auth-token }}"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json:
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 404
          # Comment: Expect a 404 status code due to an invalid URL
          think: 1

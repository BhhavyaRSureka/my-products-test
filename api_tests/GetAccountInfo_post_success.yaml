# Test generated by RoostGPT for test ArtilleryTest1 using AI Type Open AI and AI Model gpt-4


# Test generated for /GetAccountInfo_post for http method type POST in artillery framework

# RoostTestHash=8a95fbc531


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "GetAccountInfo_post_success.csv"
    fields: ["koreUserId", "botId", "accountId", "customerId"]
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20
  defaults:
    headers:
      Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
      X-Auth-token: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
      X-CORRELATION-ID: "{{$randomNumber(10000, 99999)}}"
scenarios:
  - name: "Get Account Info Scenario"
    flow:
      - post:
          url: "/GetAccountInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
          json:
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 200
            - hasProperty: "customerId"
            - hasProperty: "accounts"
            - hasProperty: "accounts[0].customerId"
            - hasProperty: "accounts[0].accountName"
            - hasProperty: "accounts[0].accountNickname"
            - hasProperty: "accounts[0].accountNumber"
            - hasProperty: "accounts[0].accountType"
            - hasProperty: "accounts[0].activatedDate"
            - hasProperty: "accounts[0].availableBalance"
          capture:
            - json: "$.customerId"
              as: "customerId"
            - json: "$.accounts[0].customerId"
              as: "accountCustomerId"
            - json: "$.accounts[0].accountName"
              as: "accountName"
            - json: "$.accounts[0].accountNickname"
              as: "accountNickname"
            - json: "$.accounts[0].accountNumber"
              as: "accountNumber"
            - json: "$.accounts[0].accountType"
              as: "accountType"
            - json: "$.accounts[0].activatedDate"
              as: "activatedDate"
            - json: "$.accounts[0].availableBalance"
              as: "availableBalance"
      - think: 1

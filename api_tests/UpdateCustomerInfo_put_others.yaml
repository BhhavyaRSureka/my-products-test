# Test generated by RoostGPT for test ArtilleryTest1 using AI Type Open AI and AI Model gpt-4


# Test generated for /UpdateCustomerInfo_put for http method type PUT in artillery framework

# RoostTestHash=89ddb90785


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "UpdateCustomerInfo_put_others.csv"
    fields:
      - "customerId"
      - "email"
      - "alternatePhoneNumber"
      - "isAlternateMobileNumber"
      - "phone"
      - "isPrimaryMobileNumber"
      - "location"
    skipHeader: true
  plugins:
    expect: {}
  environments:
    development:
      target: "http://dev.api.host"
      http:
        rejectUnauthorized: false
      headers:
        Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
        X-Auth-token: "{{$processEnvironment.AUTH_TOKEN}}"
        X-CORRELATION-ID: "{{$processEnvironment.CORRELATION_ID}}"

scenarios:
  - name: "Update Customer Info - Non-success Responses"
    flow:
      - log: "Sending incorrect payload"
        put:
          url: "/UpdateCustomerInfo"
          headers:
            koreUserId: "dummyUser"
            botId: "invalidBotId"
            accountId: "invalidAccountId"
          json:
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            alternatePhoneNumber: "{{ alternatePhoneNumber }}"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
        think: 1

      - log: "Sending request without authentication"
        put:
          url: "/UpdateCustomerInfo"
          headers:
            koreUserId: "dummyUser"
            botId: "validBotId"
            accountId: "validAccountId"
          json:
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            alternatePhoneNumber: "{{ alternatePhoneNumber }}"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
          expect:
            - statusCode: 401
        think: 1

      - log: "Sending request to invalid URL"
        put:
          url: "/InvalidURL"
          headers:
            koreUserId: "dummyUser"
            botId: "validBotId"
            accountId: "validAccountId"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            X-Auth-token: "{{$processEnvironment.AUTH_TOKEN}}"
            X-CORRELATION-ID: "{{$processEnvironment.CORRELATION_ID}}"
          json:
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            alternatePhoneNumber: "{{ alternatePhoneNumber }}"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
          expect:
            - statusCode: 404
        think: 1

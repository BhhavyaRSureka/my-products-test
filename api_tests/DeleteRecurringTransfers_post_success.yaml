# Test generated by RoostGPT for test ArtilleryTest1 using AI Type Open AI and AI Model gpt-4


# Test generated for /DeleteRecurringTransfers_post for http method type POST in artillery framework

# RoostTestHash=2abad00381


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "DeleteRecurringTransfers_post_success.csv"
    fields:
      - "customerId"
      - "paymentId"
      - "updateTransferType"
    skipHeader: true

scenarios:
  - name: "DeleteRecurringTransfers Scenario"
    flow:
      - post:
          url: "/DeleteRecurringTransfers"
          headers:
            koreUserId: "{{ $processEnvironment.KORE_USER_ID }}"
            botId: "{{ $processEnvironment.BOT_ID }}"
            userCode: "{{ $processEnvironment.USER_CODE }}"
            accountId: "{{ $processEnvironment.ACCOUNT_ID }}"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            paymentId: "{{ paymentId }}"
            updateTransferType: "{{ updateTransferType }}"
          expect:
            - statusCode: 200
            - hasProperty: "message"
          # Think time after the request
          think: 1
      - log: "Deleted recurring transfers successfully"

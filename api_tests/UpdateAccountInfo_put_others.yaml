# Test generated by RoostGPT for test ArtilleryTest1 using AI Type Open AI and AI Model gpt-4


# Test generated for /UpdateAccountInfo_put for http method type PUT in artillery framework

# RoostTestHash=adb57fbf1b


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "UpdateAccountInfo_put_others.csv"
    fields: ["accountNumber", "customerId", "email", "alternatePhoneNumber", "isAlternateMobileNumber", "phone", "isPrimaryMobileNumber", "location", "isStatement"]
    skipHeader: true
  http:
    pool: 10
  processors: {}

scenarios:
  - name: "Testing non-success API responses"
    flow:
      - log: "Sending PUT request with invalid accountNumber"
        put:
          url: "/UpdateAccountInfo"
          headers:
            koreUserId: "incorrect_user_id"
            botId: "incorrect_bot_id"
            accountId: "incorrect_account_id"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-CORRELATION-ID: 123456789
          json:
            accountNumber: "{{ accountNumber }}"
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            alternatePhoneNumber: "{{ alternatePhoneNumber }}"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
            isStatement: "{{ isStatement }}"
          expect:
            - statusCode: 400
            - hasProperty: "errorMessage"
        think: 1

      - log: "Sending PUT request with invalid auth token"
        put:
          url: "/UpdateAccountInfo"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "invalid_token"
            X-Auth-token: "invalid_token"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json:
            accountNumber: "{{ accountNumber }}"
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            alternatePhoneNumber: "{{ alternatePhoneNumber }}"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
            isStatement: "{{ isStatement }}"
          expect:
            - statusCode: 401
            - hasProperty: "errorMessage"
        think: 1

      - log: "Sending PUT request to an invalid URL"
        put:
          url: "/InvalidUrl"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-CORRELATION-ID: "{{ X-CORRELATION-ID }}"
          json:
            accountNumber: "{{ accountNumber }}"
            customerId: "{{ customerId }}"
            email: "{{ email }}"
            alternatePhoneNumber: "{{ alternatePhoneNumber }}"
            isAlternateMobileNumber: "{{ isAlternateMobileNumber }}"
            phone: "{{ phone }}"
            isPrimaryMobileNumber: "{{ isPrimaryMobileNumber }}"
            location: "{{ location }}"
            isStatement: "{{ isStatement }}"
          expect:
            - statusCode: 404
            - hasProperty: "errorMessage"
        think: 1

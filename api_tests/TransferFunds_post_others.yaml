# Test generated by RoostGPT for test ArtilleryTest1 using AI Type Open AI and AI Model gpt-4


# Test generated for /TransferFunds_post for http method type POST in artillery framework

# RoostTestHash=289ed66c26


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "TransferFunds_post_others.csv"
    fields:
      - "customerId"
      - "amount"
      - "currency"
      - "sourceAccountId"
      - "targetAccountId"
      - "date"
      - "description"
      - "TransactionType"
      - "PaymentType"
    skipHeader: true

scenarios:
  - name: "Non-success API responses scenario"
    flow:
      - log: "Sending an invalid POST request"
        post:
          url: "/InvalidTransferFunds"
          headers:
            koreUserId: "invalidUserId"
            botId: "invalidBotId"
            userCode: "invalidUserCode"
            accountId: "invalidAccountId"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            date: "{{ date }}"
            description: "{{ description }}"
            TransactionType: "{{ TransactionType }}"
            PaymentType: "{{ PaymentType }}"
          expect:
            - statusCode: 404
        think: 1

      - log: "Sending a POST request with invalid payload"
        post:
          url: "/TransferFunds"
          headers:
            koreUserId: "invalidUserId"
            botId: "invalidBotId"
            userCode: "invalidUserCode"
            accountId: "invalidAccountId"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            customerId: "invalidCustomerId"
            amount: "invalidAmount"
            currency: "invalidCurrency"
            sourceAccountId: "invalidSourceAccountId"
            targetAccountId: "invalidTargetAccountId"
            date: "invalidDate"
            description: "invalidDescription"
            TransactionType: "invalidTransactionType"
            PaymentType: "invalidPaymentType"
          expect:
            - statusCode: 400
        think: 1

      - log: "Sending a POST request without Authorization header"
        post:
          url: "/TransferFunds"
          headers:
            koreUserId: "invalidUserId"
            botId: "invalidBotId"
            userCode: "invalidUserCode"
            accountId: "invalidAccountId"
          json:
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            date: "{{ date }}"
            description: "{{ description }}"
            TransactionType: "{{ TransactionType }}"
            PaymentType: "{{ PaymentType }}"
          expect:
            - statusCode: 401
        think: 1

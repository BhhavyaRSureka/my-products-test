# Test generated by RoostGPT for test ArtilleryTest1 using AI Type Open AI and AI Model gpt-4


# Test generated for /resetPassword_post for http method type POST in artillery framework

# RoostTestHash=79c3f016d8


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "resetPassword_post_success.csv"
    fields:
      - "username"
      - "password"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20
  variables:
    AUTH_TOKEN: "{{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: "Reset Password Scenario"
    flow:
      - log: "Starting reset password scenario"
      - post:
          url: "/resetPassword"
          json:
            username: "{{ username }}"
            password: "{{ password }}"
          headers:
            Authorization: "Bearer {{ AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "message"
            - equals: 
                - "message"
                - "password has been updated successfully."
      - think: 5

# Test generated by RoostGPT for test ArtilleryTest1 using AI Type Open AI and AI Model gpt-4


# Test generated for /GetCCSchedule_post for http method type POST in artillery framework

# RoostTestHash=b38ec5fe10


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "GetCCSchedule_post_success.csv"
    fields:
      - "customerId"
      - "status"
    skipHeader: true
  environments:
    dev:
      target: "{{ $processEnvironment.API_HOST }}"
  headers:
    Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
    koreUserId: "dummyKoreUserId"
    botId: "dummyBotId"
    accountId: "dummyAccountId"
scenarios:
  - name: "GetCCSchedule API Test"
    flow:
      - post:
          url: "/GetCCSchedule"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            accountId: "{{ accountId }}"
          json:
            customerId: "{{ customerId }}"
            status: "{{ status }}"
          expect:
            - statusCode: 200
            - hasProperty: "Type"
            - hasProperty: "CustomerId"
            - hasProperty: "Amount"
            - hasProperty: "SourceAccountNumber"
            - hasProperty: "TargetAccountNumber"
            - hasProperty: "DueDate"
            - hasProperty: "Currency"
            - hasProperty: "Status"
            - hasProperty: "id"
      - think: 1

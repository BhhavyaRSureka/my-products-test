# Test generated by RoostGPT for test ArtilleryTest1 using AI Type Open AI and AI Model gpt-4


# Test generated for /GetRecurringTransfers_post for http method type POST in artillery framework

# RoostTestHash=7e03af5a84


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "GetRecurringTransfers_post_success.csv"
    fields:
      - "customerId"
      - "status"
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: Test GetRecurringTransfers API
    flow:
      - log: "Starting GetRecurringTransfers API test"
      
      - post:
          url: "/GetRecurringTransfers"
          headers:
            koreUserId: "{{ koreUserId }}"
            botId: "{{ botId }}"
            userCode: "{{ userCode }}"
            accountId: "{{ accountId }}"
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            customerId: "{{ customerId }}"
            status: "{{ status }}"
          expect:
            - statusCode: 200
            - hasProperty: "Type"
            - hasProperty: "CustomerId"
            - hasProperty: "Amount"
            - hasProperty: "SourceAccountNumber"
            - hasProperty: "TargetAccountNumber"
            - hasProperty: "Currency"
            - hasProperty: "Status"
            - hasProperty: "ScheduledDate"
            - hasProperty: "recurringOptions"
            - hasProperty: "id"

      - think: 1

      - log: "Completed GetRecurringTransfers API test"

# Test generated by RoostGPT for test ArtilleryTest1 using AI Type Open AI and AI Model gpt-4


# Test generated for /ccPay_post for http method type POST in artillery framework

# RoostTestHash=c6fa681866


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "ccPay_post_success.csv"
    fields: ["customerId", "amount", "currency", "sourceAccountId", "targetAccountId", "transactionType", "dueDate"]
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "ccPay scenario"
    flow:
      - log: "POST /ccPay"
      - post:
          url: "/ccPay"
          headers:
            koreUserId: "{{ $processEnvironment.KORE_USER_ID }}"
            botId: "{{ $processEnvironment.BOT_ID }}"
            accountId: "{{ $processEnvironment.ACCOUNT_ID }}"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            sourceAccountId: "{{ sourceAccountId }}"
            targetAccountId: "{{ targetAccountId }}"
            transactionType: "{{ transactionType }}"
            dueDate: "{{ dueDate }}"
          expect:
            - statusCode: 200
      - think: 1

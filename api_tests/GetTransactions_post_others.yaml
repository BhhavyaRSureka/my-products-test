# Test generated by RoostGPT for test ArtilleryTest1 using AI Type Open AI and AI Model gpt-4


# Test generated for /GetTransactions_post for http method type POST in artillery framework

# RoostTestHash=207beafabe


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "GetTransactions_post_others.csv"
    fields:
      - "customerId"
      - "accountNumber"
      - "category"
      - "startDate"
      - "endDate"
      - "status"
      - "transactionAmount"
  plugins:
    expect: {}
    
scenarios:
  - name: "Test Scenario - Non-Success API Responses"
    flow:
      - log: "Request: Invalid Authorization token"
        post:
          url: "/GetTransactions"
          headers:
            Authorization: "InvalidToken"
            X-Auth-token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            accountNumber: ["{{ accountNumber }}"]
            category: ["{{ category }}"]
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            status: "{{ status }}"
            transactionAmount: "{{ transactionAmount }}"
          expect:
            - statusCode: 401
        think: 1

      - log: "Request: Invalid URL"
        post:
          url: "/InvalidURL"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "{{ customerId }}"
            accountNumber: ["{{ accountNumber }}"]
            category: ["{{ category }}"]
            startDate: "{{ startDate }}"
            endDate: "{{ endDate }}"
            status: "{{ status }}"
            transactionAmount: "{{ transactionAmount }}"
          expect:
            - statusCode: 404
        think: 1

      - log: "Request: Invalid Payload"
        post:
          url: "/GetTransactions"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            X-Auth-token: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customerId: "InvalidPayload"
            accountNumber: ["InvalidPayload"]
            category: ["InvalidPayload"]
            startDate: "InvalidPayload"
            endDate: "InvalidPayload"
            status: "InvalidPayload"
            transactionAmount: "InvalidPayload"
          expect:
            - statusCode: 400
        think: 1

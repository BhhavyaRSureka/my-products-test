# Test generated by RoostGPT for test ArtilleryTest1 using AI Type Open AI and AI Model gpt-4


# Test generated for /DeleteScheduleTransfers_post for http method type POST in artillery framework

# RoostTestHash=47355f872f


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "DeleteScheduleTransfers_post_success.csv"
    fields: ["customerId","paymentId"]
    skipHeader: true
  defaults:
    headers:
      Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
scenarios:
  - name: "Delete Scheduled Transfers"
    flow:
      - post:
          url: "/DeleteScheduleTransfers"
          json:
            customerId: "{{ customerId }}"
            paymentId: "{{ paymentId }}"
          headers:
            koreUserId: "sampleUserId"
            botId: "sampleBotId"
            userCode: "sampleUserCode"
            accountId: "sampleAccountId"
          expect:
            - statusCode: 200
          # After the successful POST request, the API should return a 200 status code
      - think: 1
